{
  "name": "jest",
  "version": "1.0.0",
  "description": "testes com jest",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "jest:converage":"jest --converage",
    "backend": "json-server --watch db/db.json"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.5.0"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
